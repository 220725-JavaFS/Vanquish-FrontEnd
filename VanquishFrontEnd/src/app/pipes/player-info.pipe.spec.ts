import { PlayerInfoPipe } from './player-info.pipe';

describe('PlayerInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
